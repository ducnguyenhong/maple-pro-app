import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  imgBg: {
    width: '100%',
    height: 70,
    paddingTop: 20,
  },

  vMain: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
